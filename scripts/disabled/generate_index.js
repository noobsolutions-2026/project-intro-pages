#!/usr/bin/env node
// Disabled: moved to scripts/disabled – manual template workflow preferred.
console.log('generate_index.js is disabled. Use docs/templates/index-template.html instead.');
process.exit(0);
