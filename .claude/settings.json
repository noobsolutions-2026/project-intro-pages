{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\"file_path\".*docs/assets/style\\.css' && echo 'WARNING: docs/assets/style.css is gitignored — edits will not be committed or tracked in git.' >&2; exit 0"
          }
        ]
      }
    ]
  }
}
